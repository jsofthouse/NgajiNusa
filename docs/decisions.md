# Architecture Decisions

- Use Service Layer.
- No Repository Pattern for now.
- Use Blade instead of Livewire.
- Bootstrap 5 only.
- Authentication uses Laravel's built-in `Auth` facade (session-based), not Breeze.
- Payment uses Midtrans.
